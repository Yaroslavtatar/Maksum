# Копируйте в mail.env и заполните своими данными.
# mail.env добавлен в .gitignore и не попадёт в репозиторий.

# SMTP-сервер для отправки писем подтверждения почты
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your-mail@example.com
SMTP_PASSWORD=your-password

# Адрес отправителя (обычно совпадает с SMTP_USER)
MAIL_FROM=your-mail@example.com

# Базовый URL фронтенда для ссылки в письме (например https://vk-sosa.example.com)
# Ссылка подтверждения будет: FRONTEND_VERIFY_EMAIL_URL/verify-email?token=...
FRONTEND_VERIFY_EMAIL_URL=https://your-site.com
