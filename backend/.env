# PostgreSQL — подключение к maksum_db на сервере
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=maksum
POSTGRES_PASSWORD=K41gQ)y`fsQ|UK5F
POSTGRES_DB=maksum_db

# JWT (при необходимости раскомментируйте и задайте своё значение)
# JWT_SECRET=your_secret_change_in_production
# ACCESS_TOKEN_EXPIRE_MINUTES=60
