# ============ PostgreSQL (включить — укажите хотя бы POSTGRES_HOST или POSTGRES_URL) ============
# Если не задано — используется SQLite (backend/data/maksum.db)

POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=maksum
POSTGRES_PASSWORD=your_password_here
POSTGRES_DB=maksum_db

# Либо одна строка (пароль с символами ), `, | нужно закодировать: %29 %60 %7C):
# POSTGRES_URL=postgresql://maksum:PASSWORD@localhost:5432/maksum_db

# ============ JWT ============
# JWT_SECRET=your_secret_change_in_production
# ACCESS_TOKEN_EXPIRE_MINUTES=60

# ============ CORS (опционально) ============
# CORS_ORIGINS=http://localhost:3000,https://your-domain.com
