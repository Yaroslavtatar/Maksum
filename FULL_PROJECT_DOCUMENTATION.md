# 📚 MAKSUM - Полная документация проекта

## 🚀 Обзор проекта

**MAKSUM** - это полнофункциональный клон социальной сети VKontakte, построенный на современном стеке технологий с чистым интерфейсом, готовым к развитию.

### 🛠 Технологический стек
- **Frontend**: React 19.0.0 + Tailwind CSS
- **Backend**: FastAPI (Python) 
- **База данных**: PostgresSQL(Prodaction)+SQLite(dev build) 
- **UI Components**: ShadCN/UI + Radix UI
- **Роутинг**: React Router v7
- **Анимации**: Custom CSS + Tailwind
- **API**: Отсутвует временно

## 🌐 URL-адреса проекта

### Frontend (React)
- **Локальный адрес**: `http://localhost:3000`
- **Порт**: 3000
- **Режим разработки**: Включен hot reload

### Backend (FastAPI)
- **Локальный адрес**: `http://localhost:8001`
- **API базовый URL**: `http://localhost:8001/api`
- **Документация API**: `http://localhost:8001/docs` (Swagger)
- **Порт**: 8001 (внутренний), проксируется через Kubernetes

### База данных (MongoDB)
- **Подключение**: Через переменную окружения `POSTGRES_HOST`
- **База данных**: Определяется в `POSTGRES_DB`

## 📁 Структура проекта

```
/app/
├── frontend/                          # React приложение
│   ├── public/                        # Статические файлы
│   ├── src/
│   │   ├── components/               # React компоненты
│   │   │   ├── Auth/                 # Авторизация
│   │   │   │   └── LoginPage.jsx     # Страница входа/регистрации
│   │   │   ├── Feed/                 # Лента новостей
│   │   │   │   ├── NewsFeed.jsx      # Основная лента
│   │   │   │   └── PostCard.jsx      # Карточка поста
│   │   │   ├── Layout/               # Макет приложения
│   │   │   │   ├── Header.jsx        # Шапка с поиском и меню
│   │   │   │   ├── Sidebar.jsx       # Боковая навигация
│   │   │   │   └── MainLayout.jsx    # Основной макет
│   │   │   ├── Music/                # 🎵 Музыкальные компоненты
│   │   │   │   └── MusicPlayer.jsx   # Плеер с LastFM API (Placeholder тех работ и за проблем с тех составляющей)
│   │   │   └── ui/                   # ShadCN UI компоненты
│   │   │       ├── button.jsx        # Кнопки
│   │   │       ├── card.jsx          # Карточки
│   │   │       ├── input.jsx         # Поля ввода
│   │   │       ├── avatar.jsx        # Аватары
│   │   │       └── ... (30+ компонентов)
│   │   ├── pages/                    # Страницы приложения
│   │   │   ├── HomePage.jsx          # 🏠 Главная страница
│   │   │   ├── ProfilePage.jsx       # 👤 Профиль пользователя
│   │   │   ├── MessagesPage.jsx      # 💬 Сообщения
│   │   │   ├── FriendsPage.jsx       # 👥 Друзья
│   │   │   ├── NotificationsPage.jsx # 🔔 Уведомления
│   │   │   └── MusicPage.jsx         # 🎵 Музыка
│   │   ├── mock/                     # Моковые данные
│   │   │   └── mockData.js           # Очищенные данные для разработки
│   │   ├── hooks/                    # React хуки
│   │   │   └── use-toast.js          # Хук для уведомлений
│   │   ├── App.js                    # Главный компонент приложения
│   │   ├── App.css                   # Основные стили
│   │   └── index.css                 # Tailwind + анимации
│   ├── package.json                  # Зависимости и скрипты
│   ├── tailwind.config.js            # Конфигурация Tailwind
│   └── .env                          # Переменные окружения
├── backend/                          # FastAPI сервер
│   ├── server.py                     # Основной файл сервера
│   ├── requirements.txt              # Python зависимости
│   └── database.py                   # Полная настройка базы данных
├── README_CLEAN.md                   # Документация очищенной версии
├── MUSIC_API_SETUP.md                # Настройка музыкального API
└── FULL_PROJECT_DOCUMENTATION.md    # Этот файл
```

## 🔧 Переменные окружения

### Frontend (.env)
```env
REACT_APP_BACKEND_URL=http://localhost:8001
```

### Backend (.env)
```env
POSTGRES_HOST=localhost:5432
POSTGRES_DB=maksum_db
```

## 🎨 Компоненты интерфейса

### 🔐 Авторизация (`/components/Auth/`)
- **LoginPage.jsx**: Страница входа/регистрации в стиле VK
  - Синий градиентный фон
  - Табы для входа и регистрации
  - Валидация форм
  - Сохранение сессии в localStorage

### 🏗 Макет (`/components/Layout/`)
- **MainLayout.jsx**: Основной макет с сайдбаром и шапкой
- **Header.jsx**: Навигационная шапка
  - Поиск по сайту
  - Уведомления (5) и сообщения (3)
  - Выпадающее меню пользователя
- **Sidebar.jsx**: Боковая навигация
  - Логотип MAKSUM
  - Информация о пользователе
  - Меню разделов с бейджами
  - Счетчики непрочитанных

### 📱 Страницы (`/pages/`)

#### 🏠 **HomePage.jsx** - Главная страница
- **Левая колонка**: Друзья онлайн, популярные темы
- **Центр**: Лента новостей с формой создания поста
- **Правая колонка**: Группы, события, музыка
- **Пустые состояния**: Мотивирующие призывы к действию

#### 👤 **ProfilePage.jsx** - Профиль пользователя
- **Обложка профиля**: Фото с кнопкой изменения
- **Информация**: Имя, статус, статистика
- **Табы**: Записи, фотографии, друзья, информация
- **Боковая панель**: Контакты и статистика

#### 💬 **MessagesPage.jsx** - Сообщения
- **Левая панель**: Список диалогов с поиском
- **Правая панель**: Окно чата с сообщениями
- **Пустое состояние**: Призыв начать общение
- **Функции**: Поиск, звонки, прикрепления

#### 👥 **FriendsPage.jsx** - Друзья
- **Табы**: Все друзья, заявки, рекомендации, онлайн
- **Поиск и фильтры**: По имени и параметрам
- **Карточки друзей**: Аватар, статус, взаимные друзья
- **Пустые состояния**: Для каждой вкладки

#### 🔔 **NotificationsPage.jsx** - Уведомления
- **Типы уведомлений**: Лайки, комментарии, друзья, другое
- **Управление**: Прочитать все, настройки
- **Визуальные индикаторы**: Непрочитанные уведомления
- **Иконки**: Для каждого типа уведомлений

#### 🎵 **MusicPage.jsx** - Музыка
- **Плеер**: Текущий трек с анимацией
- **Поиск**: Через LastFM API
- **Популярное**: Рекомендуемые треки
- **Плейлисты**: Пустые для заполнения

### 🎵 Музыкальный плеер (`/components/Music/`)

#### **MusicPlayer.jsx** - Основной компонент
- **API интеграция**: LastFM для поиска треков
- **Текущий плеер**: 
  - Обложка с анимацией вращения
  - Контролы воспроизведения
  - Прогресс-бар и громкость
  - Градиентный фон
- **Поиск треков**:
  - Живой поиск через API
  - Результаты с обложками
  - Анимации появления
- **Популярные треки**: Демо-контент
- **Плейлисты**: Пустое состояние

## 🎨 Система анимаций

### CSS Keyframes (`/src/index.css`)

```css
@keyframes fade-in          # Плавное появление
@keyframes slide-up         # Слайд снизу вверх
@keyframes slide-down       # Слайд сверху вниз
@keyframes slide-left       # Слайд справа налево
@keyframes slide-right      # Слайд слева направо
@keyframes bounce-gentle    # Мягкие подпрыгивания
@keyframes pulse-gentle     # Пульсация
@keyframes spin-slow        # Медленное вращение (3s)
@keyframes scale-hover      # Увеличение при наведении
```

### CSS Classes
```css
.animate-fade-in            # Появление элементов
.animate-slide-up           # Карточки снизу
.animate-slide-down         # Элементы сверху
.animate-slide-left         # Правая колонка
.animate-slide-right        # Левая колонка
.animate-bounce-gentle      # Иконки пустых состояний
.animate-pulse-gentle       # Элементы ожидания
.animate-scale-hover        # Кнопки и интерактивные элементы
```

### Задержки анимаций
```jsx
// Поочередное появление элементов
style={{ animationDelay: `${index * 100}ms` }}
```

## 📊 Данные и состояние

### Моковые данные (`/src/mock/mockData.js`)

#### Пользователь
```javascript
mockUser = {
  id: 1,
  name: "Иван Петров",
  avatar: "...",
  status: "В сети",
  // ... остальные поля
}
```

#### Очищенные массивы
```javascript
mockPosts = []        # Пустая лента
mockFriends = []      # Нет друзей
mockMessages = []     # Нет сообщений
mockGroups = []       # Нет групп
mockNotifications = [] # Нет уведомлений
```

#### Функции-помощники
```javascript
mockCreatePost()      # Создание поста
mockSendMessage()     # Отправка сообщения
mockAddFriend()       # Добавление друга
mockLikePost()        # Лайк поста
```

## 🔐 Система авторизации

### Логика входа
1. Пользователь вводит данные на `/login`
2. При успешном входе сохраняется `localStorage.setItem('isLoggedIn', 'true')`
3. Перенаправление на главную страницу `/`

### Публичные маршруты
```jsx
// PublicRoute - перенаправляет авторизованных
const PublicRoute = ({ children }) => {
  const isLoggedIn = localStorage.getItem('isLoggedIn');
  return !isLoggedIn ? children : <Navigate to="/" />;
};
```

## 🛣 Маршрутизация

### Публичные маршруты
- `/login` - Страница входа/регистрации

### Защищенные маршруты
- `/` - Главная страница (лента)
- `/profile` - Профиль пользователя
- `/messages` - Сообщения
- `/friends` - Друзья
- `/notifications` - Уведомления
- `/music` - Музыка

### Fallback
- `*` - Перенаправление на `/`

## 🎵 API интеграции

### LastFM API
- **Ключ**: `69d1e3db24238d686cc697954b6666a1`
- **Базовый URL**: `https://ws.audioscrobbler.com/2.0/`
- **Методы**:
  - `track.search` - Поиск треков
  - `chart.gettoptracks` - Популярные треки (планируется)

### Примеры запросов
```javascript
// Поиск треков
GET https://ws.audioscrobbler.com/2.0/?method=track.search&track=QUERY&api_key=API_KEY&format=json

// Ответ содержит:
{
  results: {
    trackmatches: {
      track: [
        {
          name: "Название трека",
          artist: "Исполнитель",
          image: [
            { "#text": "url_small" },
            { "#text": "url_medium" },
            { "#text": "url_large" }
          ]
        }
      ]
    }
  }
}
```

## 🔧 Backend API

### Базовый URL
```
http://localhost:8001/api
```

### Существующие эндпоинты

#### GET `/api/`
- **Описание**: Проверка работы API
- **Ответ**: `{"message": "Hello World"}`

#### POST `/api/status`
- **Описание**: Создание записи статуса
- **Тело запроса**: `{"client_name": "string"}`
- **Ответ**: Объект StatusCheck с ID и timestamp

#### GET `/api/status`
- **Описание**: Получение всех записей статуса
- **Ответ**: Массив объектов StatusCheck

### Планируемые эндпоинты

#### Пользователи
- `GET /api/users/me` - Текущий пользователь
- `PUT /api/users/me` - Обновление профиля
- `GET /api/users/:id` - Профиль пользователя

#### Посты
- `GET /api/posts` - Лента постов
- `POST /api/posts` - Создание поста
- `POST /api/posts/:id/like` - Лайк поста
- `POST /api/posts/:id/comments` - Комментарий

#### Друзья
- `GET /api/friends` - Список друзей
- `POST /api/friends/request` - Заявка в друзья
- `POST /api/friends/accept/:id` - Принять заявку

#### Сообщения
- `GET /api/messages` - Список диалогов
- `GET /api/messages/:id` - Сообщения диалога
- `POST /api/messages` - Отправить сообщение

## 🚀 Запуск проекта

### Требования
- Node.js 18+
- Python 3.8+
- MongoDB
- Yarn или npm

### Установка и запуск

1. **Frontend**:
```bash
cd /app/frontend
yarn install
yarn start
# Доступен на http://localhost:3000
```

2. **Backend**:
```bash
cd /app/backend
pip install -r requirements.txt
uvicorn server:app --host 0.0.0.0 --port 8001 --reload
# Доступен на http://localhost:8001
```

3. **MongoDB**:
```bash
# Локально
mongod --dbpath /data/db

# Или через Docker
docker run -d -p 27017:27017 mongo
```

### Supervisor (в проекте)
```bash
# Статус сервисов
sudo supervisorctl status

# Перезапуск
sudo supervisorctl restart frontend
sudo supervisorctl restart backend
sudo supervisorctl restart all
```

## 🎯 Пустые состояния

Каждая страница имеет красиво оформленные пустые состояния:

### 🏠 Главная страница
- **Лента**: "Лента пуста" с кнопкой создания поста
- **Друзья онлайн**: "Нет друзей онлайн" с кнопкой поиска
- **Группы**: "Вы не состоите в группах"
- **События**: "Нет предстоящих событий"

### 💬 Сообщения
- **Список чатов**: "Нет сообщений" с кнопкой начала чата
- **Окно чата**: "Выберите диалог" с описанием

### 👥 Друзья
- **Все друзья**: "У вас пока нет друзей" с кнопками поиска
- **Заявки**: "Нет новых заявок"
- **Рекомендации**: "Рекомендации появятся позже"
- **Онлайн**: "Никого нет в сети"

### 🔔 Уведомления
- **Все**: "Нет уведомлений" с описанием типов
- **Друзья**: "Нет заявок в друзья"
- **Активность**: "Нет активности"
- **Другое**: "Все просмотрено!"

## 🎨 Дизайн-система

### Цвета
- **Основной**: Синий (#3B82F6) - как в VK
- **Успех**: Зеленый (#10B981)
- **Ошибка**: Красный (#EF4444)
- **Предупреждение**: Оранжевый (#F59E0B)
- **Текст**: Серый (#374151)

### Типографика
- **Заголовки**: font-bold, различные размеры
- **Основной текст**: font-medium или font-normal
- **Второстепенный**: text-gray-500
- **Мелкий текст**: text-xs, text-gray-400

### Компоненты
- **Кнопки**: Закругленные углы, hover эффекты
- **Карточки**: Тень, hover анимации
- **Поля ввода**: Фокус с синим кольцом
- **Аватары**: Круглые, с фоллбэком

### Адаптивность
- **Desktop**: Полная версия с тремя колонками
- **Tablet**: Адаптивная сетка
- **Mobile**: Мобильная навигация (планируется)

## 🔄 Состояние приложения

### Глобальное состояние
- Аутентификация через localStorage
- Данные пользователя в mockUser
- Темповые данные в компонентах

### Локальное состояние
- Формы с useState
- Модальные окна
- Поисковые запросы
- Плеер музыки

## 📱 Мобильная версия

### Текущее состояние
- Адаптивная верстка с Tailwind
- Основные компоненты подстраиваются

### Планируется
- Мобильная навигация
- Жесты для плеера
- Push уведомления
- Оптимизация для тач-устройств

## 🔍 SEO и метаданные

### Текущие настройки
- Базовый HTML с мета-тегами
- Правильная структура заголовков

### Планируется
- React Helmet для динамических мета-тегов
- Open Graph для социальных сетей
- Структурированные данные

## 🚀 Развертывание

### Локальная разработка
- Frontend: `yarn start` (порт 3000)
- Backend: `uvicorn server:app --reload` (порт 8001)
- MongoDB: Локальный или Docker

### Production (планируется)
- Frontend: Статическая сборка
- Backend: Gunicorn + Nginx
- База данных: MongoDB Atlas или VPS
- CDN для статических файлов

## 🔐 Безопасность

### Текущая реализация
- Базовая проверка авторизации
- CORS настроен на "*" (только для разработки)

### Планируется
- JWT токены
- Валидация данных
- Rate limiting
- HTTPS в production
- Защита от CSRF

## 📈 Производительность

### Оптимизации
- Lazy loading для изображений
- Виртуализация списков (планируется)
- Code splitting по маршрутам (планируется)
- Кэширование API запросов (планируется)

## 🧪 Тестирование

### Текущее состояние
- Базовая структура готова к тестированию

### Планируется
- Unit тесты с Jest + React Testing Library
- Integration тесты для API
- E2E тесты с Playwright
- Тесты анимаций

## 📊 Аналитика

### Планируется
- Google Analytics или аналогичное
- Отслеживание пользовательских действий
- Метрики производительности
- Отчеты об ошибках

## 🎯 Roadmap развития

### Фаза 1: Backend интеграция
- [ ] Полноценный API для всех функций ✔️ + -
- [ ] База данных с реальными схемами ✔️+ -
- [ ] Авторизация через JWT ✔️
- [ ] WebSocket для real-time

### Фаза 2: Расширенная функциональность
- [ ] Загрузка файлов (фото/видео) ✔️
- [ ] Группы и сообщества
- [ ] События и календарь
- [ ] Расширенный музыкальный плеер

### Фаза 3: Дополнительные возможности
- [ ] Мобильное приложение
- [ ] Push уведомления
- [ ] Темная тема ✔️
- [ ] Многоязычность

### Фаза 4: Масштабирование
- [ ] Микросервисная архитектура
- [ ] Кэширование и CDN
- [ ] Аналитика и метрики
- [ ] A/B тестирование

---

## 🎉 Заключение

**MAKSUM** представляет собой современную, полнофункциональную социальную сеть аналог для социальной сети VK. Проект готов к активной разработке и может служить отличной стартовой точкой для создания собственной социальной платформы.

**Ключевые преимущества:**
- ✅ Чистый, современный open-source

**Готово к разработке** - добавляйте функции, интегрируйте backend, создавайте уникальные возможности! 🚀
